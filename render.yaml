services:
  - type: web
    name: documind
    env: python
    buildCommand: pip install -r webapp/requirements.txt && pip install -r requirements.txt
    startCommand: cd webapp && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: OPENAI_API_KEY
        sync: false

